export default function DisplayProfile() {

    //need to fetch connected user so I can see followers and following
	return (
		<>
			<h1 style={{ color: "white" }}>Display Profile component</h1>
		</>
	);
}
